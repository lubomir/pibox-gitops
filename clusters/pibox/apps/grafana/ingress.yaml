---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  rules:
    - host: grafana-2.paas.lsedlar.cz
      http:
        paths:
          - backend:
              service:
                name: grafana
                port:
                  name: http
            pathType: ImplementationSpecific
  tls:
    - hosts:
        - grafana-2.paas.lsedlar.cz
      secretName: grafana-ingress
