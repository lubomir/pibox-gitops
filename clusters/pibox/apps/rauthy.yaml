---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rauthy
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/lubomir/pibox-gitops.git
    targetRevision: main
    path: clusters/pibox/apps/rauthy

  destination:
    server: https://kubernetes.default.svc
    namespace: rauthy

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
