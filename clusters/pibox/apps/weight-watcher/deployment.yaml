---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: weight-watcher
  namespace: weight-watcher
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weight-watcher
  template:
    metadata:
      labels:
        app: weight-watcher
    spec:
      containers:
      - env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              key: databaseUrl
              name: weightwatcher-postgres-weightwatcher-db-user
        - name: AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              key: authToken
              name: weight-watcher-secret
        image: docker.io/lsedlar/weight-watcher-rs:0.3.22@sha256:01004492ad6b21d42b2839209ff15cb85fb0cb7c8aed2f31c23da1312017c526
        imagePullPolicy: IfNotPresent
        name: weight-watcher
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          readOnlyRootFilesystem: true
        resources:
          requests:
            cpu: 10m
            memory: 64Mi
          limits:
            memory: 128Mi
        ports:
        - containerPort: 8080
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          periodSeconds: 5
          successThreshold: 1
          timeoutSeconds: 1
      terminationGracePeriodSeconds: 30
